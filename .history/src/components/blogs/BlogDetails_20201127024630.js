import React from 'react'

const BlogDetails = (props) => {
    console.log(props)
    return (
        <div className = "container">
            Hello World
        </div>
    )
}

export default BlogDetails
